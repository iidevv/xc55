{##
 # "You save" label (internal list element)
 #
 # @ListChild (list="product.plain_price.before.market_price.text", weight="200")
 #}
{{ t('you save') }} <span class="you-save">{{ this.formatPrice(this.getSaveDifference(), null, 1) }}</span>
