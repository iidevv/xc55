{##
 # Page content
 #
 # @ListChild (list="static_page.info", weight="100")
 #}

<div class="page-body-content" {{ this.printTagAttributes(this.get('page').getFieldMetadata('body'))|raw }}>{{ this.get('page').getBody()|raw }}</div>
