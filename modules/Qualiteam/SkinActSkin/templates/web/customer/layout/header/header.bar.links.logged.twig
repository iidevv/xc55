{##
 # Header bar account links
 #
 # #ListChild (list="layout.header.bar", weight="100")
 #}

{% if this.isLogged() and not this.isCheckoutLayout() %}
  <div class="header-icon-menu header-my-account-menu">
    <span class="header-icon-menu__icon-box">
      {{ widget('Qualiteam\\SkinActSkin\\View\\Icon', icon="i-user", class='header-icon-menu__icon') }}
    </span>
    <span class="header-icon-menu__label">{{ 'Account' }}</span>
    {{ include ('layout/header/header.bar.links.logged.dropdown.twig') }}
  </div>
{% endif %}
