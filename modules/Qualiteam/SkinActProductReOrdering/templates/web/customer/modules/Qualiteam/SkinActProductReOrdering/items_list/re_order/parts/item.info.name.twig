{#
 # @ListChild (list="reorder.item.info", weight="10")
#}
<p class="item-title"><a href="{{ this.getProductURL() }}">{{ this.getName() }}</a></p>

<div class="item-attribute-values">
  <ul class="selected-attribute-values">
    {% for option in this.getLastOrderItemAttributes() %}
      <li>
        <span>{{ option.getActualName() }}:</span>
        {{ option.getActualValue() }}{% if loop.index != loop.length %}, {% endif %}
      </li>
    {% endfor %}
  </ul>
</div>