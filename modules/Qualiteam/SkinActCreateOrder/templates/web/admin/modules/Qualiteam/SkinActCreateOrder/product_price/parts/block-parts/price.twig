{##
 # Wholesale prices table
 #
 # @ListChild (list="wholesale.price.widgetlist", zone="admin", weight="40")
 #}

<td>
  <span class="price-value">{{ this.formatPrice(this.wholesalePrice.displayPrice, null, 1)|raw }}</span>
  <span class="price-label">/ {{ t('each') }}</span>
</td>

