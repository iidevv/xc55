{##
 # Orders link
 #
 # @ListChild (list="slidebar.navbar.account", weight="10")
 #}

{% if this.isLogged() %}
	<li>
		<a class="icon-font icon-orders-list" href="{{ xurl('order_list', '') }}">
			<span>{{ t('Orders') }}</span>
		</a>
	</li>
{% endif %}
