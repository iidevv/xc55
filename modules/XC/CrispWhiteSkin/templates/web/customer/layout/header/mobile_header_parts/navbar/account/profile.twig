{##
 # Profile link
 #
 # @ListChild (list="slidebar.navbar.account", weight="30")
 #}

{% if this.isLogged() %}
	<li>
		<a class="icon-my-account-profile" href="{{ xurl('profile', '') }}">
			<span>{{ t('Profile details') }}</span>
		</a>
	</li>
{% endif %}
