{##
 # Review block
 #
 # @ListChild (list="reviews.review.title", weight="100")
 #}
<div class="info">
  <div class="reviewer-name">
    {% if this.get('review').getReviewerName() %}
      <span>{{ this.get('review').getReviewerName() }}</span>
    {% endif %}
    {% if not this.get('review').getReviewerName() %}
      <span>{{ t('Anonymous reviewer') }}</span>
    {% endif %}
  </div>
  <div class="date">
    {{ this.formatTime(this.get('review').getAdditionDate()) }}
  </div>
</div>
{% if this.isOwnReview(this.review) %}
  <div class="separator"></div>
{% endif %}
{% if not this.isOnModeration(this.review) %}
  <div class="approved-separator"></div>
{% endif %}
