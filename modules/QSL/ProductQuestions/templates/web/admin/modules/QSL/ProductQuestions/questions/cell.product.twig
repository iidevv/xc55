{% if this.entity.product %}
    <a href="{{ xurl('product', '', {'product_id': this.entity.product.getProductId()}) }}">{{ this.entity.product.getName() }}</a>
{% else %}
    <span class="product-deleted">{{ t('Deleted') }}</span>
{% endif %}

