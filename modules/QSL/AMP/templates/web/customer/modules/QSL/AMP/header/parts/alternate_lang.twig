{##
 # Alternate languages links
 #
 # @ListChild (list="amp.head", weight="3")
 #}

{% if this.hasAlternateLangUrls() %}
  {% for code, url in this.getAlternateLangUrls() %}
    <link rel="alternate" hreflang="{{ code }}" href="{{ url }}"/>
  {% endfor %}
{% endif %}
