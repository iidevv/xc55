<div class="reward-points-history">

    <h3>{{ t('User rewards transaction history') }}</h3>

    <table>
        {% for index, event in this.getRewardEvents() %}
          <tr>
              {{ widget_list('customer.account.rewards.history.event', index=index, event=event) }}
          </tr>
        {% endfor %}
    </table>
</div>