{##
 # Subject tempalate
 #}
{% if not this.hideCompanyInSubject %}{{ this.get('config').Company.company_name|raw }}: {% endif %}{% include [this.dir ~ '/subject.twig', 'common/default_subject.twig'] %}
