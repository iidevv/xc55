{##
 # Account link
 #
 # @ListChild (list="layout.header.bar.links.logged", weight="200")
 #}

<li class="account-link-2">
  <a href="{{ xurl('order_list', '') }}" class="register">{{ t('My account') }}</a>
  <span class="email">({{ this.get('auth').profile.login }})</span>
</li>
