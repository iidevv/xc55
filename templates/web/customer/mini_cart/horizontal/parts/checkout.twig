{##
 # Horizontal minicart checkout button block
 #
 # @ListChild (list="minicart.horizontal.buttons", weight="10")
 #}
{% if this.get('cart').checkCart() %}
  {{ widget('\\XLite\\View\\Button\\SimpleLink', label='Checkout', location=xurl('checkout'), style='regular-main-button checkout') }}
{% endif %}
{% if not this.get('cart').checkCart() %}
  {{ widget('\\XLite\\View\\Button\\SimpleLink', label='Checkout', location=xurl('checkout'), style='regular-main-button checkout', disabled='true') }}
{% endif %}
