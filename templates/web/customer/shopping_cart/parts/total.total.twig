{##
 # Shopping cart subtotal
 #
 # @ListChild (list="cart.panel.totals", weight="30")
 #}
<li class="total">
  <strong>{{ t('Total') }}: </strong>
  {{ widget('XLite\\View\\Surcharge', surcharge=this.get('cart').getTotal(), currency=this.get('cart').getCurrency()) }}
</li>
