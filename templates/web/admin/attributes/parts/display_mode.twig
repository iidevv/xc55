{##
 # Display mode
 #}

<div{% if this.get('productClass') %} data-class-id="{{ this.get('productClass').getId() }}"{% endif %} data-id="{{ this.entity.getId() }}" />
  {{ t(this.entity.getDisplayModeName()) }}
</div>
