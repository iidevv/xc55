{##
 # Subcategories count link
 #
 # @ListChild (list="itemsList.category.cell.subcategories", weight="100")
 #}

<a href="{{ xurl('categories', '', {'id': this.entity.getCategoryId()}) }}" class="count-link">{{ this.entity.getSubcategoriesCount() }}</a>
