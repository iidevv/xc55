{##
 # Entity switcher
 #}

{% if this.isAllowEntitySwitch(this.entity) %}
  {{ widget(this.switcherField.class, idx=this.get('idx'), entity=this.entity, column=this.column, itemsList=this.getSelf(), fieldName=this.switcherField.name, fieldParams=this.switcherField.params) }}
{% endif %}
