{##
 # Invoice : header : logo
 #
 # @ListChild (list="invoice.head", weight="20")
 #}
<td class="logo">
  <img src="{{ this.getInvoiceLogo() }}" alt="{{ this.get('config').Company.company_name }}" class="logo" />
</td>
